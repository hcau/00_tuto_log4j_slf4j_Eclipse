==> run as Java application
Projet Eclipse (maven).
Exemple de façade slf4j avec log4j pour journaliser (ici dans un fichier (monapp.log) et dans la sortie principale (out)).

Test tag

Utilisation de git sur Eclipse => cf : note Git Gui.odt
Utilisation du descripteur de module => cf : note Eclipse et les modules java.odt